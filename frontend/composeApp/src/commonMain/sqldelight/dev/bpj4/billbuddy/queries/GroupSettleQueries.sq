insertGroupSettles:
INSERT OR REPLACE INTO GroupSettles VALUES (?,?,?,?,?,?,?);

getLastSettleForGroup:
SELECT max(settledAt) FROM GroupSettles WHERE groupId = ? AND deletedAt ISNULL;

getGroupSettlesToSync:
SELECT * FROM GroupSettles WHERE status = ?;

clearGroupSettle:
DELETE FROM GroupSettles;